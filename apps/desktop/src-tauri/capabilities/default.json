{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal default capability: scaffold needs no IPC-exposed permissions (daemon spawning is Rust-side via ShellExt, which does not traverse the frontend permission layer \u2014 see apps/desktop/README.md hardening baseline).",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
