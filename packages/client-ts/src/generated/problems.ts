// @generated by scripts/gen-bindings.mjs from schema/*.schema.json — do not edit.
// Regenerate with `pnpm gen` at the repository root.

/**
 * The v1 seed set of coarse codes, carried forward from the TypeScript core's OmtError taxonomy. U2's pre-freeze pass subdivides these additively (e.g. ARCHIVED_READONLY under CONFLICT, DUPLICATE_MEMBER under INVALID_INPUT).
 */
export type SeedProblemCodes =
  "CONFLICT" | "INVALID_HIERARCHY" | "INVALID_INPUT" | "NOT_FOUND" | "IO" | "UNSUPPORTED_PROTOCOL" | "SCHEMA_TOO_NEW";

/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */

/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */
export interface ProblemDescriptor {
  summary: string;
  /**
   * Prose description of the Problem.details fields for this code.
   */
  detailsShape?: string;
  /**
   * Nearest coarse ancestor to interpret when this exact code is unknown to an older client; absent when the code is itself coarse.
   */
  coarseFallback?: string;
  /**
   * stable: the code will never change meaning. provisional: introduced as a subdivision and may still be folded back.
   */
  stability: "stable" | "provisional";
  [k: string]: unknown;
}
/**
 * Map of ProblemCode → descriptor. Open map: any key matching ^[A-Z][A-Z0-9_]+$ may appear.
 */

/**
 * Map of ProblemCode → descriptor. Open map: any key matching ^[A-Z][A-Z0-9_]+$ may appear.
 */
export interface ProblemRegistry {
  [k: string]: ProblemDescriptor;
}
/**
 * Seeded descriptors for the seven coarse codes.
 */

/**
 * Seeded descriptors for the seven coarse codes.
 */
export interface SeedProblemRegistry {
  CONFLICT?: ProblemDescriptor1;
  INVALID_HIERARCHY?: ProblemDescriptor2;
  INVALID_INPUT?: ProblemDescriptor3;
  NOT_FOUND?: ProblemDescriptor4;
  IO?: ProblemDescriptor5;
  UNSUPPORTED_PROTOCOL?: ProblemDescriptor6;
  SCHEMA_TOO_NEW?: ProblemDescriptor7;
}
/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */

/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */
export interface ProblemDescriptor1 {
  summary: string;
  /**
   * Prose description of the Problem.details fields for this code.
   */
  detailsShape?: string;
  /**
   * Nearest coarse ancestor to interpret when this exact code is unknown to an older client; absent when the code is itself coarse.
   */
  coarseFallback?: string;
  /**
   * stable: the code will never change meaning. provisional: introduced as a subdivision and may still be folded back.
   */
  stability: "stable" | "provisional";
  [k: string]: unknown;
}
/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */

/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */
export interface ProblemDescriptor2 {
  summary: string;
  /**
   * Prose description of the Problem.details fields for this code.
   */
  detailsShape?: string;
  /**
   * Nearest coarse ancestor to interpret when this exact code is unknown to an older client; absent when the code is itself coarse.
   */
  coarseFallback?: string;
  /**
   * stable: the code will never change meaning. provisional: introduced as a subdivision and may still be folded back.
   */
  stability: "stable" | "provisional";
  [k: string]: unknown;
}
/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */

/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */
export interface ProblemDescriptor3 {
  summary: string;
  /**
   * Prose description of the Problem.details fields for this code.
   */
  detailsShape?: string;
  /**
   * Nearest coarse ancestor to interpret when this exact code is unknown to an older client; absent when the code is itself coarse.
   */
  coarseFallback?: string;
  /**
   * stable: the code will never change meaning. provisional: introduced as a subdivision and may still be folded back.
   */
  stability: "stable" | "provisional";
  [k: string]: unknown;
}
/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */

/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */
export interface ProblemDescriptor4 {
  summary: string;
  /**
   * Prose description of the Problem.details fields for this code.
   */
  detailsShape?: string;
  /**
   * Nearest coarse ancestor to interpret when this exact code is unknown to an older client; absent when the code is itself coarse.
   */
  coarseFallback?: string;
  /**
   * stable: the code will never change meaning. provisional: introduced as a subdivision and may still be folded back.
   */
  stability: "stable" | "provisional";
  [k: string]: unknown;
}
/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */

/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */
export interface ProblemDescriptor5 {
  summary: string;
  /**
   * Prose description of the Problem.details fields for this code.
   */
  detailsShape?: string;
  /**
   * Nearest coarse ancestor to interpret when this exact code is unknown to an older client; absent when the code is itself coarse.
   */
  coarseFallback?: string;
  /**
   * stable: the code will never change meaning. provisional: introduced as a subdivision and may still be folded back.
   */
  stability: "stable" | "provisional";
  [k: string]: unknown;
}
/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */

/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */
export interface ProblemDescriptor6 {
  summary: string;
  /**
   * Prose description of the Problem.details fields for this code.
   */
  detailsShape?: string;
  /**
   * Nearest coarse ancestor to interpret when this exact code is unknown to an older client; absent when the code is itself coarse.
   */
  coarseFallback?: string;
  /**
   * stable: the code will never change meaning. provisional: introduced as a subdivision and may still be folded back.
   */
  stability: "stable" | "provisional";
  [k: string]: unknown;
}
/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */

/**
 * One registry entry: what the code means, its details shape, and coarse fallback for unknown subdivided codes.
 */
export interface ProblemDescriptor7 {
  summary: string;
  /**
   * Prose description of the Problem.details fields for this code.
   */
  detailsShape?: string;
  /**
   * Nearest coarse ancestor to interpret when this exact code is unknown to an older client; absent when the code is itself coarse.
   */
  coarseFallback?: string;
  /**
   * stable: the code will never change meaning. provisional: introduced as a subdivision and may still be folded back.
   */
  stability: "stable" | "provisional";
  [k: string]: unknown;
}
/**
 * Subdivided codes introduced by U2's pre-freeze pass (additive; each carries a coarseFallback so older clients interpret unknown codes). Open map: any key matching ^[A-Z][A-Z0-9_]+$ may appear.
 */

/**
 * Subdivided codes introduced by U2's pre-freeze pass (additive; each carries a coarseFallback so older clients interpret unknown codes). Open map: any key matching ^[A-Z][A-Z0-9_]+$ may appear.
 */
export interface SubdividedProblemRegistry {
  [k: string]: ProblemDescriptor;
}
/**
 * The subdivision entries frozen with the behavioral corpus (U2). Each property pins the exact descriptor instance registered for that code.
 */

/**
 * The subdivision entries frozen with the behavioral corpus (U2). Each property pins the exact descriptor instance registered for that code.
 */
export interface U2SubdivisionDescriptors {
  ARCHIVED_READONLY?: {
    summary: "The referenced node is archived and read-only; restore it before mutating.";
    detailsShape: "{ nodeId: string, operation: 'update' | 'report' | 'run-membership' }.";
    coarseFallback: "CONFLICT";
    stability: "provisional";
    [k: string]: unknown;
  };
  DUPLICATE_MEMBER?: {
    summary: "A run member list repeats a node id.";
    detailsShape: "{ nodeId: string } — the first duplicated id.";
    coarseFallback: "INVALID_INPUT";
    stability: "provisional";
    [k: string]: unknown;
  };
  INVALID_CONCURRENCY?: {
    summary: "Run concurrency must be a positive integer (v1 advertises runConcurrency: 1).";
    detailsShape: "{ value: number | string } — the rejected value.";
    coarseFallback: "INVALID_INPUT";
    stability: "provisional";
    [k: string]: unknown;
  };
}
