// @generated by scripts/gen-bindings.mjs from schema/*.schema.json — do not edit.
// Regenerate with `pnpm gen` at the repository root.

import type { ClaimContext, LeaseGrant, NodeSummary, NodeView, QualifiedNodeRef, RunConfig, RunItemView, RunLink, RunView, SavedFilters, TreeNode } from './common.js'
import type { EventEnvelope } from './events.js'

/**
 * Method run/control. retry/remove additionally require nodeId naming the target member.
 */
export type RunControlParams = {
  [k: string]: unknown;
} & {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  /**
   * Run identifier within one home, e.g. RUN-0001.
   */
  runId: string;
  /**
   * Run lifecycle control. retry/remove additionally require nodeId to name the target member.
   */
  action: "start" | "pause" | "resume" | "cancel" | "retry" | "remove";
  /**
   * Ticket node identifier within one home, e.g. TICKET-0001, EPIC-0003.
   */
  nodeId?: string;
  [k: string]: unknown;
};

/**
 * Conjunction of optional filters; empty object lists everything.
 */

/**
 * Conjunction of optional filters; empty object lists everything.
 */
export interface NodeFilter {
  /**
   * Ticket hierarchy node type. Hierarchy rules: epic→story, story→substory|ticket, substory→ticket, ticket→subticket; only epic may be a root.
   */
  type?: "epic" | "story" | "substory" | "ticket" | "subticket";
  /**
   * Ticket workflow status. Archived is an independent read-only dimension carried beside the status.
   */
  status?: "open" | "in_progress" | "done" | "blocked" | "skipped";
  /**
   * Read-only archival dimension: archived nodes are frozen and excluded from runs.
   */
  archived?: boolean;
  /**
   * Whitespace-separated keywords, ALL must match title/body.
   */
  query?: string;
  [k: string]: unknown;
}
/**
 * Patch applied by node/update. body replaces the whole text and is mutually exclusive with append; at least one property must be present.
 */

/**
 * Patch applied by node/update. body replaces the whole text and is mutually exclusive with append; at least one property must be present.
 */
export interface NodeChanges {
  title?: string;
  /**
   * Ticket workflow status. Archived is an independent read-only dimension carried beside the status.
   */
  status?: "open" | "in_progress" | "done" | "blocked" | "skipped";
  /**
   * Sort priority; lower numbers sort earlier. Default 0.
   */
  priority?: number;
  /**
   * Read-only archival dimension: archived nodes are frozen and excluded from runs.
   */
  archived?: boolean;
  body?: string;
  append?: string;
  [k: string]: unknown;
}
/**
 * Method node/create. Children always land in their parent's home; scope applies only to root epics.
 */

/**
 * Method node/create. Children always land in their parent's home; scope applies only to root epics.
 */
export interface CreateNodeParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  /**
   * Ticket hierarchy node type. Hierarchy rules: epic→story, story→substory|ticket, substory→ticket, ticket→subticket; only epic may be a root.
   */
  type: "epic" | "story" | "substory" | "ticket" | "subticket";
  title: string;
  /**
   * Ticket node identifier within one home, e.g. TICKET-0001, EPIC-0003.
   */
  parentId?: string;
  body?: string;
  /**
   * Sort priority; lower numbers sort earlier. Default 0.
   */
  priority?: number;
  /**
   * Where a root epic is created: the workspace (.omt/) home or the global per-user home. Only meaningful when parentId is absent.
   */
  scope?: "workspace" | "global";
  [k: string]: unknown;
}

export interface CreateNodeResult {
  node: NodeView;
  [k: string]: unknown;
}
/**
 * Full qualified view of a ticket node as persisted.
 */

/**
 * Method node/get.
 */
export interface GetNodeParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  /**
   * Ticket node identifier within one home, e.g. TICKET-0001, EPIC-0003.
   */
  nodeId: string;
  [k: string]: unknown;
}
/**
 * Detail view: node, optional parent, children, user-owned body, and non-terminal runs holding this ticket.
 */

/**
 * Detail view: node, optional parent, children, user-owned body, and non-terminal runs holding this ticket.
 */
export interface GetNodeResult {
  node: NodeView;
  parent?: NodeSummary;
  children: NodeSummary[];
  body: string;
  runs: RunLink[];
  [k: string]: unknown;
}
/**
 * Lightweight qualified view of a ticket node.
 */

/**
 * Method node/list.
 */
export interface ListNodeParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  filter?: NodeFilter;
  [k: string]: unknown;
}

export interface ListNodeResult {
  nodes: NodeSummary[];
  [k: string]: unknown;
}
/**
 * Method node/tree: full forest, or the subtree under rootId.
 */

/**
 * Method node/tree: full forest, or the subtree under rootId.
 */
export interface TreeParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  /**
   * Ticket node identifier within one home, e.g. TICKET-0001, EPIC-0003.
   */
  rootId?: string;
  [k: string]: unknown;
}

export interface TreeResult {
  trees: TreeNode[];
  [k: string]: unknown;
}
/**
 * Recursive tree projection: a node summary plus its ordered subtree.
 */

/**
 * Method node/search (@-trigger candidates). Empty query yields latest nodes first.
 */
export interface SearchNodesParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  query?: string;
  limit?: number;
  [k: string]: unknown;
}

export interface SearchNodesResult {
  nodes: NodeSummary[];
  [k: string]: unknown;
}
/**
 * Method node/update.
 */

/**
 * Method node/update.
 */
export interface UpdateNodeParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  /**
   * Ticket node identifier within one home, e.g. TICKET-0001, EPIC-0003.
   */
  nodeId: string;
  /**
   * Optimistic-concurrency revision counter of a node or run.
   */
  expectedRevision?: number;
  changes: NodeChanges;
  [k: string]: unknown;
}

export interface UpdateNodeResult {
  node: NodeView;
  [k: string]: unknown;
}
/**
 * Method node/move: move the node with its whole subtree under newParentId (same home only).
 */

/**
 * Method node/move: move the node with its whole subtree under newParentId (same home only).
 */
export interface MoveNodeParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  /**
   * Ticket node identifier within one home, e.g. TICKET-0001, EPIC-0003.
   */
  nodeId: string;
  /**
   * Ticket node identifier within one home, e.g. TICKET-0001, EPIC-0003.
   */
  newParentId: string;
  [k: string]: unknown;
}

export interface MoveNodeResult {
  node: NodeView;
  [k: string]: unknown;
}
/**
 * Method node/archive: set the read-only archival dimension. Unarchive goes through node/update changes.archived=false.
 */

/**
 * Method node/archive: set the read-only archival dimension. Unarchive goes through node/update changes.archived=false.
 */
export interface ArchiveNodeParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  /**
   * Ticket node identifier within one home, e.g. TICKET-0001, EPIC-0003.
   */
  nodeId: string;
  [k: string]: unknown;
}

export interface ArchiveNodeResult {
  node: NodeView;
  [k: string]: unknown;
}
/**
 * Method node/execute: mark in_progress and bind the caller's execution mark (the UI execute button). Executor identity derives from the connection principal, never from payload fields (R12).
 */

/**
 * Method node/execute: mark in_progress and bind the caller's execution mark (the UI execute button). Executor identity derives from the connection principal, never from payload fields (R12).
 */
export interface ExecuteNodeParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  /**
   * Ticket node identifier within one home, e.g. TICKET-0001, EPIC-0003.
   */
  nodeId: string;
  [k: string]: unknown;
}

export interface ExecuteNodeResult {
  node: NodeView;
  [k: string]: unknown;
}
/**
 * Method home/reindex: rebuild the SQLite index from Markdown after hand edits.
 */

/**
 * Method home/reindex: rebuild the SQLite index from Markdown after hand edits.
 */
export interface ReindexHomeParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  [k: string]: unknown;
}
/**
 * Outcome of a full reindex from Markdown to SQLite.
 */

/**
 * Outcome of a full reindex from Markdown to SQLite.
 */
export interface ReindexHomeResult {
  nodes: number;
  edges: number;
  /**
   * Entries skipped because they were illegal or unparseable.
   */
  skipped: number;
  [k: string]: unknown;
}
/**
 * Method run/create: ordered batch of Ticket/SubTicket members; every member must belong to homeId.
 */

/**
 * Method run/create: ordered batch of Ticket/SubTicket members; every member must belong to homeId.
 */
export interface RunCreateParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  title?: string;
  /**
   * @minItems 1
   */
  nodeIds: [string, ...string[]];
  config?: RunConfig;
  [k: string]: unknown;
}
/**
 * Execution configuration of a run; omitted keys take documented defaults (stopOnFailure=false, autoContinue=true, autoVerify=false, concurrency=1).
 */

export interface RunCreateResult {
  run: RunView;
  items: RunItemView[];
  [k: string]: unknown;
}
/**
 * Full qualified view of a run with derived progress.
 */

/**
 * Method run/get.
 */
export interface RunGetParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  /**
   * Run identifier within one home, e.g. RUN-0001.
   */
  runId: string;
  [k: string]: unknown;
}

export interface RunGetResult {
  run: RunView;
  items: RunItemView[];
  [k: string]: unknown;
}
/**
 * Method run/list: active, interrupted and history runs of one home.
 */

/**
 * Method run/list: active, interrupted and history runs of one home.
 */
export interface RunListParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  /**
   * Lifecycle state of a run batch.
   */
  status?: "pending" | "running" | "paused" | "completed" | "completed_with_failures" | "canceled" | "interrupted";
  [k: string]: unknown;
}

export interface RunListResult {
  runs: RunView[];
  [k: string]: unknown;
}

export interface RunControlResult {
  run: RunView;
  item?: RunItemView;
  [k: string]: unknown;
}
/**
 * Method run/claim: atomically claim the next pending item; executor binds to the connection principal's actor namespace (R12); agent-less callers cannot claim.
 */

/**
 * Method run/claim: atomically claim the next pending item; executor binds to the connection principal's actor namespace (R12); agent-less callers cannot claim.
 */
export interface ClaimRunParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  /**
   * Run identifier within one home, e.g. RUN-0001.
   */
  runId: string;
  [k: string]: unknown;
}
/**
 * claimed=false when no pending member remains; otherwise lease+item+ticket+context are present.
 */

/**
 * claimed=false when no pending member remains; otherwise lease+item+ticket+context are present.
 */
export interface ClaimRunResult {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  /**
   * Run identifier within one home, e.g. RUN-0001.
   */
  runId: string;
  claimed: boolean;
  lease?: LeaseGrant;
  item?: RunItemView;
  ticket?: NodeView;
  context?: ClaimContext;
  contextError?: string;
  [k: string]: unknown;
}
/**
 * Fenced execution lease returned by a successful claim (R10). Reports must echo the token; expiry demotes the claim.
 */

/**
 * Method run/report: report one claimed item's outcome; lease-fenced when a lease token is supplied (R10).
 */
export interface ReportRunParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  /**
   * Run identifier within one home, e.g. RUN-0001.
   */
  runId: string;
  /**
   * Ticket node identifier within one home, e.g. TICKET-0001, EPIC-0003.
   */
  nodeId: string;
  /**
   * Executor-reported outcome for one claimed run item.
   */
  outcome: "done" | "failed" | "blocked" | "skipped";
  note?: string;
  leaseToken?: string;
  [k: string]: unknown;
}

export interface ReportRunResult {
  run: RunView;
  item: RunItemView;
  node: NodeView;
  [k: string]: unknown;
}
/**
 * Method events/resume: fetch committed events after a stored cursor (F4); omit cursor to start from the oldest retained event.
 */

/**
 * Method events/resume: fetch committed events after a stored cursor (F4); omit cursor to start from the oldest retained event.
 */
export interface EventsResumeParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  cursor?: number;
  limit?: number;
  [k: string]: unknown;
}
/**
 * resync=true signals retention expiry: drop local projections and take a snapshot instead.
 */

/**
 * resync=true signals retention expiry: drop local projections and take a snapshot instead.
 */
export interface EventsResumeResult {
  cursor: number;
  resync: boolean;
  events: EventEnvelope[];
  [k: string]: unknown;
}
/**
 * One durably committed event of one home.
 */

/**
 * Method ui/filters-get. Adapter-only channel; key is the adapter-scoped bag key (e.g. DSH session id). Missing/corrupt bags fall back to defaults.
 */
export interface FiltersGetParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  key: string;
  [k: string]: unknown;
}

export interface FiltersGetResult {
  filters: SavedFilters;
  [k: string]: unknown;
}
/**
 * Opaque UI filter bag persisted per principal. The daemon stores it without interpreting it; the schema only pins it to an object.
 */

/**
 * Method ui/filters-set: merge patch onto the saved bag; merged result validated server-side.
 */
export interface FiltersSetParams {
  /**
   * Stable public identifier of one OMT home. Every public reference in the protocol must be qualified with a HomeId (R4). Bare filesystem/workspace paths are DSH-adapter compatibility inputs and are resolved to a HomeId before anything crosses the wire.
   */
  homeId: string;
  key: string;
  filters: SavedFilters;
  [k: string]: unknown;
}

export interface FiltersSetResult {
  filters: SavedFilters;
  [k: string]: unknown;
}
/**
 * Method ui/recent-get: recently-touched qualified node refs for one adapter key. Recent bags are global-home scoped, not per workspace home.
 */

/**
 * Method ui/recent-get: recently-touched qualified node refs for one adapter key. Recent bags are global-home scoped, not per workspace home.
 */
export interface RecentGetParams {
  key: string;
  [k: string]: unknown;
}
/**
 * Most-recent-first qualified ids.
 */

/**
 * Most-recent-first qualified ids.
 */
export interface RecentGetResult {
  refs: QualifiedNodeRef[];
  [k: string]: unknown;
}
/**
 * Method ui/recent-set: replace the bag (most-recent-first order preserved).
 */

/**
 * Method ui/recent-set: replace the bag (most-recent-first order preserved).
 */
export interface RecentSetParams {
  key: string;
  /**
   * @maxItems 100
   */
  refs: QualifiedNodeRef[];
  [k: string]: unknown;
}

export interface RecentSetResult {
  refs: QualifiedNodeRef[];
  [k: string]: unknown;
}
