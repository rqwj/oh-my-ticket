[package]
name = "omt-domain"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Pure OMT domain model: hierarchy/status/run/lease-decision semantics ported from src/host/core.ts (plan U3); zero harness dependencies."

[dependencies]
omt-contracts = { path = "../omt-contracts" }
serde = { workspace = true }
serde_json = { workspace = true }
